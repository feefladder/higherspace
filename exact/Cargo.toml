[package]
name = "exact"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
fraction = { git = "https://github.com/dnsl48/fraction", version = "0.14.0" }
